/* Brain sync 3.5.64 — sincronizzazione unica, acquisti congelati e budget coerente */
(function(){
'use strict';
const VERSION='3.5.64',ROLES=['P','D','C','A'];
let syncing=false,wrapped=false;
function read(){try{return JSON.parse(localStorage.getItem('AF_CURRENT')||'null')}catch(e){return null}}
function save(s){try{if(typeof current!=='undefined')current=s}catch(e){}try{localStorage.setItem('AF_CURRENT',JSON.stringify(s));const db=JSON.parse(localStorage.getItem('AF_DB')||'[]'),i=db.findIndex(x=>Number(x.id)===Number(s.id));if(i>=0){db[i]=s;localStorage.setItem('AF_DB',JSON.stringify(db))}}catch(e){}}
function mine(s){return(s?.teams||[]).find(t=>Number(t.id)===Number(s.myTeamId))||(s?.teams||[])[0]||null}
function roleOf(p){const r=String(p?.role||'').trim().toUpperCase();return ROLES.includes(r)?r:null}
function slots(st,r){const n=Number(st?.slots?.[r]);return Number.isFinite(n)&&n>=0?Math.round(n):0}
function base(st){st.baseAllocation=st.baseAllocation&&typeof st.baseAllocation==='object'?st.baseAllocation:{...(st.allocation||{})};ROLES.forEach(r=>st.baseAllocation[r]=Math.max(0,Number(st.baseAllocation[r])||0));return st.baseAllocation}
function planned(s,st,r){return Math.round((Number(s.initialCredits)||1200)*base(st)[r]/100)}
function spent(s,r){return(mine(s)?.players||[]).filter(p=>roleOf(p)===r).reduce((n,p)=>n+Math.max(0,Number(p.price)||0),0)}
function ensure(st,r,n){const keys=[['slotTargets',()=>({priority:'base',playerId:null})],['slotBudgets',()=>0],['slotBudgetManual',()=>false],['slotFrozen',()=>null],['slotAllocation',()=>0]];keys.forEach(([k,make])=>{st[k]=st[k]&&typeof st[k]==='object'?st[k]:{};st[k][r]=Array.isArray(st[k][r])?st[k][r]:[];while(st[k][r].length<n)st[k][r].push(make());st[k][r]=st[k][r].slice(0,n)})}
function exact100(values){const out={},raw=[];let total=0;ROLES.forEach(r=>total+=Math.max(0,Number(values[r])||0));if(total<=0){ROLES.forEach(r=>out[r]=0);return out}ROLES.forEach(r=>{const x=Math.max(0,Number(values[r])||0)/total*100;out[r]=Math.floor(x);raw.push({r,f:x-Math.floor(x)})});let left=100-ROLES.reduce((n,r)=>n+out[r],0);raw.sort((a,b)=>b.f-a.f||ROLES.indexOf(a.r)-ROLES.indexOf(b.r));for(let i=0;i<left;i++)out[raw[i%raw.length].r]++;return out}
function freeze(st,r,i,p){const price=Math.max(0,Number(p?.price)||0);st.slotFrozen[r][i]={playerId:p.id,name:p.name,price,role:r};st.slotTargets[r][i]={...(st.slotTargets[r][i]||{}),playerId:p.id};st.slotBudgets[r][i]=price;st.slotBudgetManual[r][i]=false}
function redistribute(st,s,r){const n=slots(st,r);if(!n)return;const plan=planned(s,st,r),f=st.slotFrozen[r],old=st.slotAllocation[r],a=new Array(n).fill(0),fixed=[];let fixedPct=0;for(let i=0;i<n;i++)if(f[i]){const p=plan>0?Math.max(0,Number(f[i].price)||0)/plan*100:0;fixed.push({i,p});fixedPct+=p}if(fixedPct>100){const q=fixedPct?100/fixedPct:0;fixed.forEach(x=>a[x.i]=Math.round(x.p*q));st.slotAllocation[r]=a;return}fixed.forEach(x=>a[x.i]=Math.round(x.p));let remain=Math.max(0,100-a.reduce((x,y)=>x+y,0));const free=[];for(let i=0;i<n;i++)if(!f[i]){const w=Number(old[i]);free.push({i,w:Number.isFinite(w)&&w>0?w:1})}if(free.length){const total=free.reduce((x,y)=>x+y.w,0),parts=[];let used=0;free.forEach(x=>{const q=remain*x.w/total,b=Math.floor(q);a[x.i]=b;used+=b;parts.push({i:x.i,f:q-b})});parts.sort((x,y)=>y.f-x.f);for(let i=0;i<remain-used;i++)a[parts[i%parts.length].i]++}else if(fixed.length&&remain)a[fixed[fixed.length-1].i]+=remain;st.slotAllocation[r]=a;for(let i=0;i<n;i++)if(f[i])st.slotBudgets[r][i]=f[i].price;else if(!st.slotBudgetManual[r][i])st.slotBudgets[r][i]=Math.round(plan*a[i]/100)}
function recover(st,s,r){const n=slots(st,r),t=st.slotTargets[r],f=st.slotFrozen[r],owned=(mine(s)?.players||[]).filter(p=>roleOf(p)===r),used=new Set();for(let i=0;i<n;i++)if(f[i]?.playerId!=null)used.add(String(f[i].playerId));const free=[];for(let i=0;i<n;i++)if(!f[i])free.push(i);let changed=false;owned.filter(p=>!used.has(String(p.id))).forEach(p=>{if(!free.length)return;const wanted=String(p.priority||t[free[0]]?.priority||'base'),j=free.findIndex(i=>String(t[i]?.priority||'base')===wanted),i=free.splice(j>=0?j:0,1)[0];freeze(st,r,i,p);changed=true});redistribute(st,s,r);return changed}
function sync(){if(syncing)return false;const s=read();if(!s||!Array.isArray(s.brainStrategies))return false;syncing=true;let changed=false;try{s.brainStrategies.forEach(st=>{base(st);ROLES.forEach(r=>{const n=slots(st,r);if(!n)return;ensure(st,r,n);const f=st.slotFrozen[r],t=st.slotTargets[r];for(let i=0;i<n;i++){const pid=f[i]?.playerId??t[i]?.playerId;if(pid!=null){const own=(mine(s)?.players||[]).find(p=>String(p.id)===String(pid));if(own&&!f[i]){freeze(st,r,i,own);changed=true}}if(f[i]&&!((mine(s)?.players||[]).some(p=>String(p.id)===String(f[i].playerId)))){f[i]=null;changed=true}}if(recover(st,s,r))changed=true;st.liveRoleBudget=st.liveRoleBudget||{};st.liveRoleBudget[r]=Math.max(0,planned(s,st,r)-spent(s,r));});});if(changed)save(s)}finally{syncing=false}return changed}
function live(s,st){const vals={};ROLES.forEach(r=>vals[r]=Math.max(0,planned(s,st,r)-spent(s,r)));st.liveRoleBudget=vals;st.liveAllocation=exact100(vals);return vals}
function patchDOM(s,st){const rows=[...document.querySelectorAll('#brainContent .brain-role-row')];rows.forEach(row=>{const m=row.className.match(/\brole-([PDCA])\b/);if(!m)return;const r=m[1],input=row.querySelector('.brain-percent input');if(input)input.value=String(st.liveAllocation?.[r]??0);const nums=row.querySelectorAll('.brain-role-numbers span');const b=nums[2]?.querySelector('b');if(b)b.textContent=String(st.liveRoleBudget?.[r]??0);const f=st.slotFrozen?.[r]||[],a=st.slotAllocation?.[r]||[],bud=st.slotBudgets?.[r]||[],plan=planned(s,st,r);[...row.querySelectorAll('.brain-slot')].forEach((slot,i)=>{const fr=f[i],bp=slot.querySelector('.brain-slot-budget'),pp=slot.querySelector('.brain-slot-percent input'),pl=slot.querySelector('.brain-slot-player');if(fr){if(bp)bp.textContent=String(fr.price);if(pp){pp.value=String(plan>0?Math.round(fr.price/plan*100):0);pp.disabled=true}if(pl){pl.textContent=fr.name;pl.classList.add('has-player');pl.setAttribute('aria-disabled','true');if('disabled' in pl)pl.disabled=true;pl.removeAttribute('onclick')}slot.classList.add('is-frozen','purchased')}else{if(bp)bp.textContent=String(Math.max(0,Number(bud[i])||Math.round(plan*(Number(a[i])||0)/100)));if(pp){pp.value=String(a[i]||0);pp.disabled=false}if(pl){pl.removeAttribute('aria-disabled');if('disabled' in pl)pl.disabled=false}slot.classList.remove('is-frozen','purchased')}}})})}
function setVersion(){const v=document.querySelector('.app-version');if(v)v.textContent='V. '+VERSION}
function render(){const s=read(),st=s?.brainStrategies?.find(x=>Number(x.id)===Number(s.activeBrainStrategyId));if(!s||!st)return;sync();const fresh=read(),fs=fresh?.brainStrategies?.find(x=>Number(x.id)===Number(fresh.activeBrainStrategyId));if(!fresh||!fs)return;live(fresh,fs);patchDOM(fresh,fs);setVersion()}
function wrap(){if(typeof window.renderBrain==='function'&&!window.renderBrain.__brainSync64){const old=window.renderBrain;window.renderBrain=function(){const out=old.apply(this,arguments);setTimeout(render,0);return out};window.renderBrain.__brainSync64=true}if(typeof window.assignPlayer==='function'&&!window.assignPlayer.__brainSync64){const old=window.assignPlayer;window.assignPlayer=function(){const out=old.apply(this,arguments);setTimeout(render,80);return out};window.assignPlayer.__brainSync64=true}if(!wrapped){wrapped=true;setTimeout(render,80)}}
function boot(){setVersion();sync();wrap();setTimeout(render,120)}
if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',boot,{once:true});else boot();window.BrainSyncFix={version:VERSION,sync,refresh:render};
})();